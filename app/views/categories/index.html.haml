//.right.controls= link_to "New Forum Category", new_category_path
// .module
//    .module_header
//      = category.title
//      %span.controls.right.smaller
//        = link_to "New Forum", new_forum_path
//        = link_to "Edit Category", edit_category_path(category)
//        = link_to "Delete Category", category_path(category), :confirm => "Are you sure you want to delete this category?", :method => :delete 
.guides.guide.row
  .sidebar.span3
    %ul.nav
      %li.top
      %li.meta
        .left 
          Author
      %li.meta
        .left Date
      %li.meta
        .left Category
        %li.list
          %div 
            %h1 Recent posts
            %ul
              %li
                Ha
      %li
        %a.btn.submit{:href => "#"} New topic
  .span9
    .page
      .body
        - @categories.each do |category|
          - if category.forums.size > 0
            %div
              %table.table
                %tr.smaller
                  %th{:align => "left", :colspan => "2"} Forum
                  %th Topics
                  %th Posts
                  %th.last_post{:align => "left"} Last Post
                - category.forums.each do |forum|
                  %tr
                    %td.icon= image_tag 'ruby.png'
                    %td.description
                      = link_to forum.title, forum_path(forum)
                      %br/
                      %span.smaller= forum.description
                      %br/
                    %td.counts.smaller= forum.topics.size
                    %td.counts.smaller= forum.posts.size - forum.topics.size
                    %td.last_post.smaller
                      - if forum.posts.size > 0
                        = forum.posts.last.created_at
                        %br/
                        = forum.posts.last.user.username
                      - else
                        No Topics / Posts
          - else
            .module_body There are currently no forums. 

